{
  "name": "auth0-golang-web-app-tests",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  },
  "scripts": {
    "test": "npx playwright test --config=playwright.config.js"
  }
}
